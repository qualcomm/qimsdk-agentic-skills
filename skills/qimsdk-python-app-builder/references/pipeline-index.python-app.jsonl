{"prompt": "Two-pipeline application-managed buffer bridge Python app using qimsdk AppSrc and AppSink. Pipeline 1 generates synthetic videotestsrc ball-pattern frames at NV12/1920x1080/30fps into an AppSink with set_buffer_consumer() forwarding buffers. Pipeline 2 receives them via AppSrc (do-timestamp, block, TIME format) and renders fullscreen on waylandsink. Demonstrates cross-pipeline buffer bridging via application code rather than a single linked pipeline.", "path": "python-app/qimsdk_test_appsrc_and_appsink.py", "task": "appsink-frame-capture"}
{"prompt": "Audio classification Python app (YAMNet) over an MP4-muxed FLAC track, with the video and a classification label panel composited side-by-side on a Wayland display. qtdemux splits video (h264 decode -> NV12 -> qtivcomposer sink_0) and audio (flacparse -> flacdec -> audioconvert -> audioresample -> audiobuffersplit -> qtimlaconverter feature=lmfe -> qtimltflite(yamnet_float) -> qtimlpostprocess(module=yamnet) -> VideoFilter(RGBA) -> qtivcomposer sink_1). Label panel positioned via composer input(1).set('position',[50,50])/('dimensions',[368,64]).", "path": "python-app/qimsdk_test_audio_classification_yamnet.py", "task": "audio-classification"}
{"prompt": "Audio classification Python app (YAMNet, CPU runtime, no delegate) over an MP4-muxed FLAC track, video plus classification-label overlay composited on a Wayland display. Same topology as the HTP variant but qtimltflite runs on CPU (no external delegate). Audio LMFE features via qtimlaconverter (sample-rate=16000, nfft=96,nhop=160,nmels=64,chunklen=0.96); label panel rendered RGBA into qtivcomposer with list-form pad geometry.", "path": "python-app/qimsdk_test_audio_classification_yamnet_cpu.py", "task": "audio-classification"}
{"prompt": "isp-camera preview Python app: captures live frames from the default Qualcomm camera (qtiqmmfsrc), normalizes to NV12/1920x1080/30fps via VideoFilter stream filter, and renders fullscreen on waylandsink with sync disabled for live camera. No AI, no encode, no tee branches. This is the canonical minimal camera-preview shape.", "path": "python-app/qimsdk_test_camera.py", "task": "camera-preview"}
{"prompt": "Camera capture encoded to MP4 file Python app, no display, no AI. Captures isp-camera (qtiqmmfsrc) frames at NV12/1920x1080/30fps, encodes via v4l2h264enc (dmabuf io modes), parses H.264, muxes into mp4mux, writes to filesink with eos handling enabled. This is the canonical camera-to-file record shape with no preview branch.", "path": "python-app/qimsdk_test_camera_encoder.py", "task": "video-record"}
{"prompt": "This is the base/canonical single-stage YOLOv8 object detection Python app: qimsdk Pipeline camera app running YOLOv8 detection on frames from the default isp-camera, then displaying results. Captures qtiqmmfsrc camera=0 at NV12/1920x1080/30fps, tees into a qtimlvconverter to qtimltflite (external delegate, htp) to qtimlpostprocess (module=yolov8) branch, merges detection metadata with qtimetamux, overlays boxes via qtivoverlay, renders fullscreen on waylandsink. Explicit-style Element/Pipeline construction with named link() call.", "path": "python-app/qimsdk_test_camera_yolov8.py", "task": "object-detection"}
{"prompt": "Fused ML-bin YOLOv8 object detection Python app from isp-camera to display, using the single qimsdk MLVideoTFLiteBin wrapper instead of discrete qtimlvconverter/qtimltflite/qtimlpostprocess stages. Captures qtiqmmfsrc camera=0 at NV12/1920x1080/30fps, runs qtimlvideotflitebin (inference-delegate=external, htp, postprocess-module=yolov8) directly in the media path, overlays via qtivoverlay, displays on waylandsink. Use for the compact ML-bin camera detection shape rather than discrete-stage detection.", "path": "python-app/qimsdk_test_camera_yolov8_mlbin.py", "task": "object-detection"}
{"prompt": "Two-stage detection-classification daisy-chain Python app: decodes an MP4 file with the Qualcomm hardware decoder, Stage 1 runs YOLOX (yolox_w8a8.tflite) object detection on full frames (qtimlpostprocess module=yolov8), then crops detected object ROIs and Stage 2 runs MobileNet-v2 (mobilenet_v2_w8a8.tflite) classification on each ROI (module=mobilenet) via qtimltflite on the HTP/NPU. Overlays both detection boxes and per-ROI classification labels and renders fullscreen on waylandsink. Canonical ROI-based detect-then-classify daisy chain.", "path": "python-app/qimsdk_test_daisychain_detection_classification.py", "task": "daisy-chain-multi-task"}
{"prompt": "Two-stage pose-estimation daisy-chain Python app: decodes an MP4 file with the Qualcomm hardware decoder, Stage 1 runs person/foot detection (person_foot_detection_w8a8.tflite, qtimlpostprocess module=qpd) on full frames, then crops person ROIs and Stage 2 runs HRNet pose estimation (hrnetpose_w8a8.tflite, module=hrnet, roi-batch-cumulative mode) on each ROI via qtimltflite on the HTP/NPU. Overlays skeleton keypoints on detected persons and renders fullscreen on waylandsink. Canonical ROI-batch detect-then-pose daisy chain.", "path": "python-app/qimsdk_test_daisychain_detection_pose.py", "task": "pose-estimation"}
{"prompt": "Event-triggered recording Python app: run YOLOv8 object detection on an mp4 file, display the overlay, and record the video to /tmp/event-output.mp4 only while a person is present. Dual-pipeline: main pipeline detects (qtimlpostprocess->qtimlmetaparser json->AppSink gate) and tees the overlaid video to a second AppSink (isolated with qtivtransform); a resident recording pipeline (appsrc is-live/do-timestamp/format=TIME fixed NV12 caps -> queue -> v4l2h264enc -> h264parse -> mp4mux -> filesink) is kept PLAYING and fed copied buffers only while the person-presence gate is on. Person detected via object_detection/label JSON keys.", "path": "python-app/qimsdk_test_event_encoder_record.py", "task": "event-triggered-record"}
{"prompt": "Object detection Python app from mp4-file to display with a custom Python postprocess callback instead of the built-in qtimlpostprocess module. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec, tees into a qtimlvconverter to qtimltflite (yolov8 model) branch feeding a qimsdk MLPostprocess element with set_handler() implementing manual tensor decode, NMS, and detection population, merges metadata with qtimetamux, overlays via qtivoverlay, displays on waylandsink. Use when the postprocess decode logic must be hand-written in Python rather than using a built-in module.", "path": "python-app/qimsdk_test_external_postprocess_detection_yolov8.py", "task": "video-postprocess"}
{"prompt": "Two-stage pose estimation daisy-chain Python app from mp4-file to display: stage 1 person/object detection (qtimltflite, yolov5m model) with a custom Python MLPostprocess callback producing detection boxes, stage 2 HRNet pose estimation (qtimltflite, hrnet_pose model) on stage-1 ROIs with a second custom Python MLPostprocess callback producing keypoints. Both stages use discrete qtimlvconverter/qtimltflite stages, tee/qtimetamux metadata merge per stage, final qtivoverlay renders skeleton keypoints on waylandsink. Use for hand-written pose decode logic instead of a built-in pose postprocess module.", "path": "python-app/qimsdk_test_external_postprocess_pose_estimation_hrnet.py", "task": "pose-estimation"}
{"prompt": "Object detection Python app from mp4-file to display with a custom Python preprocess callback inside the fused ML-bin, using qimsdk MLVideoTFLiteBin.set_preprocess_handler() with preprocess-engine=none instead of a discrete MLVConverter. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec, mlbin runs the custom NV12-to-tensor conversion plus yolov8 inference and built-in postprocess, overlays via qtivoverlay, displays on waylandsink. Use for hand-written ML-bin preprocess instead of a discrete external-preprocess pipeline.", "path": "python-app/qimsdk_test_external_preprocess_detection_mlbin_yolov8.py", "task": "video-preprocess"}
{"prompt": "Object detection Python app from mp4-file to display with a custom Python preprocess callback replacing the built-in qtimlvconverter tensor conversion. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec, tees into a qimsdk MLVConverter (engine=none) with set_handler() implementing manual NV12-to-tensor conversion, feeds qtimltflite (yolov8 model) and the built-in qtimlpostprocess module, merges metadata with qtimetamux, overlays via qtivoverlay, displays on waylandsink. Use when the image-to-tensor conversion math must be hand-written in Python.", "path": "python-app/qimsdk_test_external_preprocess_detection_yolov8.py", "task": "video-preprocess"}
{"prompt": "Single-stream face detection Python app: decodes an MP4 file with the Qualcomm hardware decoder, runs Face Detection Lite (face_det_lite.tflite) inference on full frames via qtimltflite on the HTP/NPU external delegate, decodes detections with qtimlpostprocess, overlays bounding boxes on detected faces, and renders fullscreen on waylandsink. Canonical full-frame face detection shape.", "path": "python-app/qimsdk_test_face_detection.py", "task": "face-detection"}
{"prompt": "Hand gesture recognition Python app from isp-camera to display: four-stage daisy-chain with palm detection (qtimltflite, palmd module, gpu delegate), qtimetatransform module=roi-palmd for ROI propagation, hand landmark estimation (hlandmark module), gesture embedding (tensor passthrough), and gesture classification (mobilenet module), merged across two qtimetamux stages, overlaid via qtivoverlay onto waylandsink. Explicit-style multi-stage tee/queue wiring named stage_01 through stage_03. Use for the shipped gesture-recognition topology, not a generic N-stage cascade.", "path": "python-app/qimsdk_test_gesture_recognition.py", "task": "gesture-recognition"}
{"prompt": "Single-stream image classification Python app: decodes an MP4 file with the Qualcomm hardware decoder (qtdemux, h264parse, v4l2h264dec), normalizes to NV12, then tees into a display branch and an ML branch. The ML branch runs MobileNet-v2 (mobilenet_v2_w8a8.tflite) classification on full frames via qtimltflite on the HTP/NPU external delegate, decodes the tensor with qtimlpostprocess module=mobilenet, and overlays the top label and confidence via qtimetamux + qtivoverlay before rendering fullscreen on waylandsink. Canonical full-frame classification shape.", "path": "python-app/qimsdk_test_image_classification_mobilenet.py", "task": "image-classification"}
{"prompt": "YOLOv8 object-detection metadata-parser Python app from an mp4 file: renders the bounding-box overlay on a Wayland display AND taps the raw detection metadata into an AppSink for application-side consumption. Decode -> NV12 -> tee; AI branch qtimlvconverter -> qtimltflite(yolov8_det_w8a8, external HTP) -> qtimlpostprocess(module=yolov8) fans out via a tee: one leg TextFilter->qtimetamux->qtivoverlay->display, the other qtimlmetaparser(module=json)->AppSink whose set_buffer_consumer reads the JSON via Buffer.data().", "path": "python-app/qimsdk_test_metadata_parser_appsink.py", "task": "object-detection"}
{"prompt": "Two-stage PPE personal protective equipment daisy-chain Python app using fused ML-bin stages from mp4-file to display. Stage 1 person-foot detection using qtimlvideotflitebin with postprocess-module=qpd and model foot_track_net on full frames. Stage 2 PPE gear detection using qtimlvideotflitebin with postprocess-module=yolov8 and model gear_guard_net on stage-1 person ROIs, preprocess-mode=roi-batch-cumulative. Uses v4l2h264dec hardware decode, qtivoverlay renders both stages results, waylandsink fullscreen display. Direct mlbin1 to mlbin2 to overlay to display fused chain, no external tee/metamux metadata branch.", "path": "python-app/qimsdk_test_mlbin_ppe.py", "task": "daisy-chain-multi-task"}
{"prompt": "Non-fused ML-bin YOLOv8 object detection Python app from mp4-file to display using qimsdk MLVideoTFLiteBin. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec hardware decode with dma io modes, normalizes to NV12, runs qtimlvideotflitebin (inference-delegate=external, htp, postprocess-module=yolov8) directly in the media path, overlays via qtivoverlay, renders on waylandsink fullscreen. File-source variant of the fused ML-bin detection shape.", "path": "python-app/qimsdk_test_mlbin_yolov8.py", "task": "object-detection"}
{"prompt": "Monocular depth estimation Python app (MiDaS-v2) with side-by-side comparison on a Wayland display. Decode -> NV12 -> tee; passthrough to qtivcomposer (left pane); AI branch qtimlvconverter -> qtimltflite(midas_v2_w8a8, external HTP) -> qtimlpostprocess(module=midas-v2, labels=monodepth.json colormap) -> VideoFilter(RGBA, no pinned resolution) -> qtivcomposer (right pane). Composer input(N) position/dimensions place the two 960x1080 panes. Depth colormap labels file required.", "path": "python-app/qimsdk_test_monodepth_sidebyside.py", "task": "monodepth"}
{"prompt": "Canonical multistream Python app: decode one MP4 file into 32 independent simultaneous streams, run non-batched YOLOX object detection on each stream with one qtimlvconverter -> qtimltflite (external HTP/NPU) -> qtimlpostprocess (module=yolov8) chain per stream, overlay bounding boxes, and compose all streams into a dynamic 6x6 grid with qtivcomposer for Wayland display. Supports display as the primary output, with optional MP4 file or RTSP output modes. Uses per-stream queues, qtimetamux/qtivoverlay, composer input(N).set position/dimensions lists, and raises the file-descriptor limit for the high stream count.", "path": "python-app/qimsdk_test_multistream_inference_grid.py", "task": "multistream-video"}
{"prompt": "Single ISP camera object detection with simultaneous multi-output Python app: captures qtiqmmfsrc camera-0 frames at NV12, runs YOLOv5 (yolov5_float.tflite) detection via qtimltflite on the HTP/NPU external delegate, overlays bounding boxes (qtimlpostprocess module=yolov8), then tees the overlaid stream to THREE simultaneous sinks at once — waylandsink display, an MP4 file (v4l2h264enc + mp4mux + filesink), and an RTSP re-stream (v4l2h264enc + h264parse + qtirtspbin on port 8900). Canonical camera detection with concurrent display+file+RTSP fan-out.", "path": "python-app/qimsdk_test_object_detection_multi_output.py", "task": "object-detection"}
{"prompt": "Single-stream object detection to file Python app: decodes an MP4 file with the Qualcomm hardware decoder, runs YOLOX (yolox_w8a8.tflite) object detection on full frames via qtimltflite on the HTP/NPU external delegate, decodes detections with qtimlpostprocess module=yolov8, overlays bounding boxes and class labels, then encodes the overlaid video with v4l2h264enc and muxes to an output MP4 filesink. Canonical decode-detect-encode-to-file shape with no display branch.", "path": "python-app/qimsdk_test_object_detection_yolox_encode.py", "task": "object-detection"}
{"prompt": "Parallel inferencing Python app: one decode teed into four independent AI branches composed into a 2x2 grid on a Wayland display. Branch A image classification (mobilenet), B pose (hrnet), C object detection (yolov8/YOLOX), all via qtimlpostprocess->TextFilter->qtimetamux->qtivoverlay; branch D semantic segmentation (deeplab-argmax) rendered RGBA and alpha-blended via its own qtivcomposer. All four tiles feed one top-level qtivcomposer. Because the composers hold tee buffers, each overlay branch's passthrough leg inserts qtivtransform ! NV12 for sole buffer ownership. Composer geometry via input(N).set('position',[x,y])/('dimensions',[w,h]).", "path": "python-app/qimsdk_test_parallel_inference_wall.py", "task": "multi-task-grid"}
{"prompt": "Two-stage HRNet pose estimation daisy-chain Python app from an mp4 file to a Wayland display. Stage 1 runs person/foot detection (person_foot_detection_w8a8, module=qpd, qtimlvconverter mode=image-batch-non-cumulative) on full frames; its ROIs feed stage 2 HRNet pose (hrnetpose_w8a8, module=hrnet, qtimlvconverter mode=roi-batch-cumulative). Each stage's text metadata is merged back with its own qtimetamux, then qtivoverlay draws the skeleton keypoints. HRNet is top-down and MUST run as this cascade — single-stage pose on full frames yields no keypoints. External HTP/NPU delegate.", "path": "python-app/qimsdk_test_pose_estimation_hrnet_cascade.py", "task": "pose-estimation"}
{"prompt": "MP4 file decode-to-display Python app using qtdemux/h264parse/v4l2h264dec hardware decode, no AI. Reads local mp4-file, demuxes and parses H.264, decodes via hardware v4l2h264dec with dma io modes, normalizes to NV12, renders fullscreen on waylandsink. Uses the start()/wait()/stop() staged lifecycle instead of execute(). This is the canonical multimedia-only file playback shape with no ML/tee/overlay branches.", "path": "python-app/qimsdk_test_qtdemux_decode_display.py", "task": "video-playback"}
{"prompt": "Single-stream DeepLabV3+ semantic segmentation Python app from an mp4 file, alpha-blended over the original video on a Wayland display. Decode -> NV12 -> tee; passthrough branch to qtivcomposer sink_0; AI branch qtimlvconverter -> qtimltflite(deeplabv3_plus_mobilenet_w8a8, external HTP) -> qtimlpostprocess(module=deeplab-argmax) -> VideoFilter(RGBA, no pinned resolution) -> qtivcomposer sink_1 with alpha=0.5. Rendered mask is RGBA (not BGRA) and unpinned so composer sink-pad dimensions scale it.", "path": "python-app/qimsdk_test_segmentation_deeplab.py", "task": "segmentation"}
{"prompt": "Super-resolution Python app (QuickSRNet) with side-by-side comparison on a Wayland display. Decode -> NV12 -> tee; passthrough to qtivcomposer (left, original); AI branch qtimlvconverter -> qtimltflite(quicksrnetlarge_w8a8, external HTP) -> qtimlpostprocess(module=srnet) -> VideoFilter(RGBA, no pinned resolution) -> qtivcomposer (right, upscaled). Composer input(N) position/dimensions define the two panes. Render format is RGBA (srnet emits {RGBA,RGBx}).", "path": "python-app/qimsdk_test_super_resolution_sidebyside.py", "task": "super-resolution"}
{"prompt": "Tensor dump / inspection Python app from mp4-file, no display, no postprocess. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec hardware decode, normalizes to NV12, runs qtimlvconverter to produce a raw model-input tensor (TensorFilter type=UINT8, dimensions 1x520x520x3), writes the tensor buffer to a raw file via multifilesink instead of running inference. Use for capturing/inspecting the exact preprocessed tensor a model would receive, without running qtimltflite.", "path": "python-app/qimsdk_test_tensor_dump.py", "task": "video-preprocess"}
{"prompt": "USB/V4L2 camera preview Python app: captures a live USB UVC webcam via v4l2src with a device-node argument (default /dev/video2), applies qtivtransform, restricts the stream to NV12/1920x1080/30fps via VideoFilter, renders fullscreen on waylandsink with sync disabled. This is the canonical usb-camera preview shape, distinct from the isp-camera qtiqmmfsrc preview app.", "path": "python-app/qimsdk_test_usb_camera.py", "task": "camera-preview"}
{"prompt": "Synthetic test video source with string-form StreamFilter caps and geometric transform, Python app to display. Generates videotestsrc ball-pattern frames, applies qtivtransform rotate=180, uses raw caps-string qimsdk StreamFilter objects (video/x-raw,format=NV12,width=1920,height=1080,framerate=30/1 then a resize caps string) instead of the VideoFilter builder, renders on waylandsink fullscreen. Use when raw GStreamer caps strings are needed instead of the typed VideoFilter wrapper.", "path": "python-app/qimsdk_test_videotestsrc_streamfilter_display.py", "task": "generic-camera-demo"}
{"prompt": "Runs a QIM SDK Python pipeline entirely from an external YAML configuration file passed as a command-line argument, with no hand-coded topology in main.py. Reads the YAML file into a string, constructs a qimsdk Pipeline directly from the YAML config text via the two-argument Pipeline constructor, and executes it. Use for the YAML-config-driven Python app pattern where the pipeline topology and properties live entirely in an external YAML file rather than being expressed as Element/Pipeline calls.", "path": "python-app/qimsdk_test_yml.py", "task": "wayland-composition"}
{"prompt": "12-stream batched YOLOv8 object detection Python app: 12 independent MP4 decode chains split into 3 batch groups of 4 streams each. Each group runs qtivtransform(NV12) -> qtibatch -> qtimlvconverter -> qtimltflite(batch-4 model, external HTP, round-robin htp_device_id across cores) -> qtimldemux, with demux outputs linked in the same order as batch inputs. Each stream's per-batch qtimlpostprocess(module=yolov8) RGBA detection layer is composited over that same stream's own original NV12 passthrough at identical position/dimensions -- two interleaved qtivcomposer input pads per stream (passthrough then detection, alpha-blended) -- arranged in a 4x3 grid on waylandsink (sync=False). Raises the file-descriptor limit for the 12 concurrent decode chains.", "path": "python-app/qimsdk_test_batched_multistream_yolov8_grid.py", "task": "multistream-video"}

# Skills Guidance

This directory contains skill payloads.

- Each direct child directory under `skills/` is a skill.
- Every implemented skill directory must contain `SKILL.md`.
- Runtime references needed by a skill should stay inside that skill directory.
- Do not add a separate `prod/` or `dev/` layer unless repository policy changes.
- Do not add `AGENTS.md` files inside individual skill directories.

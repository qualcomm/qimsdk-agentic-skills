{"prompt": "Two-pipeline application-managed buffer bridge C++ app using qti::AppSrc and qti::AppSink. Pipeline 1 generates synthetic videotestsrc ball-pattern frames at NV12/1920x1080/30fps into an AppSink with set_buffer_consumer() forwarding buffers. Pipeline 2 receives them via AppSrc (do-timestamp, block, TIME format) and renders fullscreen on waylandsink. Demonstrates cross-pipeline buffer bridging via application code rather than a single linked pipeline.", "path": "cpp-app/qimsdk_test_appsrc_and_appsink", "task": "appsink-frame-capture"}
{"prompt": "Audio classification C++ app over an MP4-muxed FLAC track: qtdemux splits concurrent video and audio branches, queues each dynamic pad before parsing/decoding, decodes H.264 video and FLAC audio, converts audio to 16 kHz LMFE features with qtimlaconverter, runs YAMNet CPU inference and qtimlpostprocess(module=yamnet, results=3), renders an RGBA classification panel into qtivcomposer, and displays the video plus panel on Wayland. Uses queue isolation after demux and FLAC decode, 31200-byte audiobuffersplit, and fixed composer panel geometry.", "path": "cpp-app/qimsdk_test_audio_classification_yamnet", "task": "audio-classification"}
{"prompt": "CPU YAMNet audio classification C++ app over an MP4-muxed FLAC track. Queues concurrent qtdemux video/audio branches and the decoded audio path, extracts 16 kHz LMFE features, runs qtimltflite with delegate=none, renders a fixed RGBA 368x64 label panel, and composites it over decoded H.264 video with qtivcomposer for Wayland. Does not set composer pad position/dimensions because that combination deadlocks CPU audio preroll; uses runtime-expanded HOME paths.", "path": "cpp-app/qimsdk_test_audio_classification_yamnet_cpu", "task": "audio-classification"}
{"prompt": "isp-camera preview C++ app: captures live frames from the default Qualcomm camera (qtiqmmfsrc), normalizes to NV12/1920x1080/30fps via VideoFilter stream filter, and renders fullscreen on waylandsink with sync disabled for live camera. No AI, no encode, no tee branches. This is the canonical minimal camera-preview shape.", "path": "cpp-app/qimsdk_test_camera", "task": "camera-preview"}
{"prompt": "Camera capture encoded to MP4 file C++ app, no display, no AI. Captures isp-camera (qtiqmmfsrc) frames at NV12/1920x1080/30fps, encodes via v4l2h264enc (dmabuf io modes), parses H.264, muxes into mp4mux, writes to filesink with eos handling enabled. This is the canonical camera-to-file record shape with no preview branch.", "path": "cpp-app/qimsdk_test_camera_encoder", "task": "video-record"}
{"path": "cpp-app/qimsdk_test_camera_yolov5_multi_output", "task": "snapshot-multi-output", "prompt": "ISP camera YOLOv5 object-detection C++ app fanning one overlaid stream out to three simultaneous sinks: Wayland display, an MP4 file, and an RTSP mount point (rtspsrc-served rtsp://<device-ip>:8900/live). Captures qticamsrc camera=0 at NV12/1920x1080/30fps, runs YOLOv5 (mapped to qtimlpostprocess module=yolov8 per model-catalog) with bbox-stabilization on HTP/NPU, merges detection metadata with qtimetamux, overlays boxes with qtivoverlay, then tees the overlaid stream into a display branch (sync=false) plus two independent v4l2h264enc(capture-io-mode=4,output-io-mode=5 for camera-native DMA import) encode branches feeding filesink and qtirtspbin respectively. Use for camera-source detection with simultaneous display+file+RTSP output."}
{"prompt": "This is the base/canonical single-stage YOLOv8 object detection C++ app: qti::Pipeline camera app running YOLOv8 detection on frames from the default isp-camera, then displaying results. Captures qtiqmmfsrc camera=0 at NV12/1920x1080/30fps, tees into a qtimlvconverter to qtimltflite (external delegate, htp) to qtimlpostprocess (module=yolov8) branch, merges detection metadata with qtimetamux, overlays boxes via qtivoverlay, renders fullscreen on waylandsink. Explicit-style Element/Pipeline construction with named link() call.", "path": "cpp-app/qimsdk_test_camera_yolov8", "task": "object-detection"}
{"prompt": "Fused ML-bin YOLOv8 object detection C++ app from isp-camera to display, using the single qti::MLVideoTFLiteBin wrapper instead of discrete qtimlvconverter/qtimltflite/qtimlpostprocess stages. Captures qtiqmmfsrc camera=0 at NV12/1920x1080/30fps, runs qtimlvideotflitebin (inference-delegate=external, htp, postprocess-module=yolov8) directly in the media path, overlays via qtivoverlay, displays on waylandsink. Use for the compact ML-bin camera detection shape rather than discrete-stage detection.", "path": "cpp-app/qimsdk_test_camera_yolov8_mlbin", "task": "object-detection"}
{"path": "cpp-app/qimsdk_test_daisychain_detection_classification", "task": "daisy-chain-multi-task", "prompt": "Two-stage daisy-chain C++ app from an MP4 file to Wayland display: stage 1 runs YOLOX object detection (qtimlvconverter mode=image-batch-non-cumulative, qtimlpostprocess module=yolov8) on full frames and merges its metadata with metamux_1; stage 2 runs MobileNet-v2 classification (qtimlvconverter mode=roi-batch-cumulative, qtimlpostprocess module=mobilenet) on the stage-1 detected ROIs and merges its metadata with metamux_2; a single qtivoverlay draws both bounding boxes and per-object classification labels. Two-mux two-tee topology for detect-then-classify daisy chains (as opposed to detect-then-pose, which uses the HRNet cascade shape)."}
{"prompt": "Event-triggered recording C++ app: decode an MP4 file, run YOLOX object detection on the HTP/NPU, display the detection overlay, and record the composed annotated video to an MP4 only while a person is present. The metadata branch uses qtimlpostprocess text/x-raw constrained by TextFilter and a qti::AppSink gate; a resident is-live qti::AppSrc recording pipeline stays PLAYING and receives moved DMA-backed qti::Buffer frames only while the gate is active, then finalizes mp4mux with EOS. Uses qtimlpostprocess module=yolov8, qtivcomposer, NV12 encoder output, and capture/output IO mode 4/4. HOME-relative paths are resolved at runtime. Use for event-triggered recording and metadata-gated AppSrc/AppSink bridges.", "path": "cpp-app/qimsdk_test_event_encoder_record", "task": "event-triggered-record"}
{"prompt": "Object detection C++ app from mp4-file to display with a custom C++ postprocess callback instead of the built-in qtimlpostprocess module. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec, tees into a qtimlvconverter to qtimltflite (yolov8 model) branch feeding a qti::MLPostprocess element with set_handler() implementing manual tensor decode, NMS, and MLDetection population, merges metadata with qtimetamux, overlays via qtivoverlay, displays on waylandsink. Use when the postprocess decode logic must be hand-written in C++ rather than using a built-in module.", "path": "cpp-app/qimsdk_test_external_postprocess_detection_yolov8", "task": "video-postprocess"}
{"prompt": "Two-stage pose estimation daisy-chain C++ app from mp4-file to display: stage 1 person/object detection (qtimltflite, yolov5m model) with a custom C++ MLPostprocess callback producing detection boxes, stage 2 HRNet pose estimation (qtimltflite, hrnet_pose model) on stage-1 ROIs with a second custom C++ MLPostprocess callback producing keypoints. Both stages use discrete qtimlvconverter/qtimltflite stages, tee/qtimetamux metadata merge per stage, final qtivoverlay renders skeleton keypoints on waylandsink. Use for hand-written pose decode logic instead of a built-in pose postprocess module.", "path": "cpp-app/qimsdk_test_external_postprocess_pose_estimation_hrnet", "task": "pose-estimation"}
{"prompt": "Object detection C++ app from mp4-file to display with a custom C++ preprocess callback inside the fused ML-bin, using qti::MLVideoTFLiteBin.set_preprocess_handler() with preprocess-engine=none instead of a discrete MLVConverter. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec, mlbin runs the custom NV12-to-tensor conversion plus yolov8 inference and built-in postprocess, overlays via qtivoverlay, displays on waylandsink. Use for hand-written ML-bin preprocess instead of a discrete external-preprocess pipeline.", "path": "cpp-app/qimsdk_test_external_preprocess_detection_mlbin_yolov8", "task": "video-preprocess"}
{"prompt": "Object detection C++ app from mp4-file to display with a custom C++ preprocess callback replacing the built-in qtimlvconverter tensor conversion. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec, tees into a qti::MLVConverter (engine=none) with set_handler() implementing manual NV12-to-tensor conversion, feeds qtimltflite (yolov8 model) and the built-in qtimlpostprocess module, merges metadata with qtimetamux, overlays via qtivoverlay, displays on waylandsink. Use when the image-to-tensor conversion math must be hand-written in C++.", "path": "cpp-app/qimsdk_test_external_preprocess_detection_yolov8", "task": "video-preprocess"}
{"path": "cpp-app/qimsdk_test_face_detection_qfd", "task": "face-detection", "prompt": "Face detection C++ app from an MP4 file to Wayland display using Face Detection Lite (qtimlpostprocess module=qfd) on HTP/NPU external delegate. Standard single-stage tee/qtimlvconverter/qtimltflite/qtimlpostprocess/TextFilter/qtimetamux/qtivoverlay overlay shape drawing face bounding boxes. Plain face detection only \u2014 does not chain into landmark/3DMM/recognition stages; use the face-recognition daisy-chain pattern (qfd -> lite-3dmm -> qfr) instead when the request asks for identity/recognition, not just detection."}
{"prompt": "Hand gesture recognition C++ app from isp-camera to display: four-stage daisy-chain with palm detection (qtimltflite, palmd module, gpu delegate), qtimetatransform module=roi-palmd for ROI propagation, hand landmark estimation (hlandmark module), gesture embedding (tensor passthrough), and gesture classification (mobilenet module), merged across two qtimetamux stages, overlaid via qtivoverlay onto waylandsink. Explicit-style multi-stage tee/queue wiring named stage_01 through stage_03. Use for the shipped gesture-recognition topology, not a generic N-stage cascade.", "path": "cpp-app/qimsdk_test_gesture_recognition", "task": "gesture-recognition"}
{"path": "cpp-app/qimsdk_test_image_classification_discrete", "task": "image-classification", "prompt": "Basic single-stage image classification C++ app from an MP4 file to Wayland display: filesrc/qtdemux/h264parse/v4l2h264dec decode, NV12 tee into passthrough and AI branches, discrete qtimlvconverter/qtimltflite/qtimlpostprocess(module=mobilenet) inference on HTP/NPU external delegate, qtimetamux merges the top classification label/confidence text metadata with the passthrough video, qtivoverlay draws it, waylandsink displays fullscreen. Canonical single-stage classification-overlay shape."}
{"path": "cpp-app/qimsdk_test_image_segmentation_composer", "task": "segmentation", "prompt": "Semantic segmentation C++ app from an MP4 file to Wayland display using DeepLabV3+ Mobilenet (module=deeplab-argmax). Tees decoded NV12 video into a passthrough branch feeding qtivcomposer sink_0 directly and an AI branch (qtimlvconverter/qtimltflite HTP/qtimlpostprocess) whose rendered RGBA mask feeds sink_1 with alpha=0.5 blended over the passthrough. No qtimetamux/qtivoverlay because segmentation is a video-output-only postprocess module. Canonical direct-to-composer segmentation shape for a single overlay (not a multi-tile grid)."}
{"path": "cpp-app/qimsdk_test_metadata_extraction_appsink", "task": "metadata-extraction", "prompt": "C++ app that both overlays and app-side-parses AI detection metadata from an MP4 file: after qtimlpostprocess(module=yolov8) produces serialized text/x-raw detection metadata, a second tee (meta_split) fans the metadata into a display branch (TextFilter -> qtimetamux -> qtivoverlay -> waylandsink) and an application-side qti::AppSink branch whose set_buffer_consumer callback receives the raw serialized buffer for app-specific parsing (placeholder logs buffer size; real parsing of the serialized GstStructure is a TODO). Use when the request needs both a visible overlay and programmatic access to detection metadata, not just one or the other."}
{"prompt": "Two-stage PPE personal protective equipment daisy-chain C++ app using fused ML-bin stages from mp4-file to display. Stage 1 person-foot detection using qtimlvideotflitebin with postprocess-module=qpd and model foot_track_net on full frames. Stage 2 PPE gear detection using qtimlvideotflitebin with postprocess-module=yolov8 and model gear_guard_net on stage-1 person ROIs, preprocess-mode=roi-batch-cumulative. Uses v4l2h264dec hardware decode, qtivoverlay renders both stages results, waylandsink fullscreen display. Direct mlbin1 to mlbin2 to overlay to display fused chain, no external tee/metamux metadata branch.", "path": "cpp-app/qimsdk_test_mlbin_ppe", "task": "daisy-chain-multi-task"}
{"prompt": "Non-fused ML-bin YOLOv8 object detection C++ app from mp4-file to display using qti::MLVideoTFLiteBin. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec hardware decode with dma io modes, normalizes to NV12, runs qtimlvideotflitebin (inference-delegate=external, htp, postprocess-module=yolov8) directly in the media path, overlays via qtivoverlay, renders on waylandsink fullscreen. File-source variant of the fused ML-bin detection shape.", "path": "cpp-app/qimsdk_test_mlbin_yolov8", "task": "object-detection"}
{"prompt": "Monocular depth estimation C++ app (MiDaS-v2) with side-by-side comparison on a Wayland display. Decodes an MP4 file through qtdemux, h264parse, v4l2h264dec and a post-decoder queue, normalizes to NV12, then tees the video into a passthrough composer pane and a qtimlvconverter -> qtimltflite (midas_v2_w8a8, external HTP/NPU) -> qtimlpostprocess(module=midas-v2, monodepth.json labels) rendered-video branch. The rendered branch uses VideoFilter(RGBA) without pinned resolution and feeds qtivcomposer; composer input pads set vector position/dimensions for the 960x1080 panes. HOME-relative filesystem paths are expanded in C++ before assigning element properties. Displays the composed 1920x1080 result on waylandsink.", "path": "cpp-app/qimsdk_test_monodepth_sidebyside", "task": "monodepth"}
{"path": "cpp-app/qimsdk_test_multistream_yolox_grid_nonbatched", "task": "multistream-grid", "prompt": "32-stream non-batched YOLOX object-detection AI wall C++ app: one MP4 file replicated into 32 fully independent decode/tee/qtimlvconverter/qtimltflite/qtimlpostprocess/qtimetamux/qtivoverlay branches (own inference instance per stream, no qtibatch/qtimldemux sharing), each feeding a dedicated qtivcomposer sink pad tiled into a 6x6 grid (ceil(sqrt(32))) on waylandsink with sync=false. Raises the process file-descriptor limit (setrlimit RLIMIT_NOFILE=10000) before pipeline construction because many independent file-decode branches exhaust the default fd limit. Use for a high-stream-count independent-inference AI wall; use the batched-inference pattern instead when the request calls for shared/batched inference across streams."}
{"path": "cpp-app/qimsdk_test_object_detection_file_output", "task": "object-detection", "prompt": "Object detection C++ app from an MP4 file to an output MP4 file (no display): decodes with the hardware H.264 decoder, runs YOLOX detection (qtimlpostprocess module=yolov8, confidence 51.0) on HTP/NPU, overlays boxes with qtimetamux/qtivoverlay, re-encodes the overlaid NV12 video with v4l2h264enc (capture/output-io-mode=4/4 for a decoded-file source), muxes with mp4mux, and writes to filesink with eos(true) to finalize the MP4. Use for file-in/file-out AI overlay requests instead of a display sink."}
{"prompt": "Parallel inferencing C++ app: one MP4 decode tees into four independent HTP/NPU AI branches composed into a 2x2 Wayland grid. Classification, top-down qpd\u2192ROI\u2192HRNet pose, and YOLOX detection use separate passthrough buffer-copy branches with qtimetamux/TextFilter/qtivoverlay; DeepLabV3 segmentation uses a local qtivcomposer to alpha-blend an RGBA mask over a copied passthrough tile before feeding the top-level four-input grid. Uses concrete qpd companion assets, fixed 1920x1080 NV12 input, htp_performance_mode=2, and matching composer geometry.", "path": "cpp-app/qimsdk_test_parallel_inference_wall", "task": "multi-task-grid"}
{"prompt": "Top-down HRNet pose estimation C++ app from an MP4 file to Wayland display: stage 1 person-foot detection using person_foot_detection_w8a8 with qtimlpostprocess module=qpd and qtimlvconverter mode=image-batch-non-cumulative generates person ROIs; stage 1 metadata merges with the video, then stage 2 qtimlvconverter mode=roi-batch-cumulative feeds hrnetpose_w8a8 with module=hrnet and hrnet settings, merges stage-2 pose metadata, and qtivoverlay renders skeleton keypoints. Uses external HTP/NPU inference, runtime-expanded HOME paths, and two qtimetamux stages. Use this cascade rather than full-frame HRNet when subjects need ROI pose detection.", "path": "cpp-app/qimsdk_test_pose_estimation_hrnet_cascade", "task": "pose-estimation"}
{"prompt": "MP4 file decode-to-display C++ app using qtdemux/h264parse/v4l2h264dec hardware decode, no AI. Reads local mp4-file, demuxes and parses H.264, decodes via hardware v4l2h264dec with dma io modes, normalizes to NV12, renders fullscreen on waylandsink. Uses the start()/wait()/stop() staged lifecycle instead of execute(). This is the canonical multimedia-only file playback shape with no ML/tee/overlay branches.", "path": "cpp-app/qimsdk_test_qtdemux_decode_display", "task": "video-playback"}
{"prompt": "Super-resolution C++ app (QuickSRNetLarge) with side-by-side comparison on a Wayland display. Decodes an MP4 file through qtdemux, h264parse, v4l2h264dec and a post-decoder queue, normalizes to NV12, then tees the video into an original passthrough composer pane and a qtimlvconverter -> qtimltflite (quicksrnetlarge_w8a8, external HTP/NPU) -> qtimlpostprocess(module=srnet) rendered-video branch. The SR render branch uses VideoFilter(RGBA) and composer input pads set vector position/dimensions for 960x1080 panes; HOME-relative input/model paths are expanded in C++ before element properties. Displays the composed original and upscaled frames on waylandsink.", "path": "cpp-app/qimsdk_test_super_resolution_sidebyside", "task": "super-resolution"}
{"prompt": "Tensor dump / inspection C++ app from mp4-file, no display, no postprocess. Decodes MP4/H.264 via filesrc, qtdemux, h264parse, v4l2h264dec hardware decode, normalizes to NV12, runs qtimlvconverter to produce a raw model-input tensor (TensorFilter type=UINT8, dimensions 1x520x520x3), writes the tensor buffer to a raw file via multifilesink instead of running inference. Use for capturing/inspecting the exact preprocessed tensor a model would receive, without running qtimltflite.", "path": "cpp-app/qimsdk_test_tensor_dump", "task": "video-preprocess"}
{"prompt": "USB/V4L2 camera preview C++ app: captures a live USB UVC webcam via v4l2src with a device-node argument (default /dev/video2), applies qtivtransform, restricts the stream to NV12/1920x1080/30fps via VideoFilter, renders fullscreen on waylandsink with sync disabled. This is the canonical usb-camera preview shape, distinct from the isp-camera qtiqmmfsrc preview app.", "path": "cpp-app/qimsdk_test_usb_camera", "task": "camera-preview"}
{"prompt": "USB camera C++ app with YOLOv8 object detection and simultaneous multi-output: captures a UVC v4l2src YUY2 stream, converts it through qtivtransform to NV12, runs qtimlvconverter -> qtimltflite external HTP/NPU -> qtimlpostprocess(module=yolov8) with qtimetamux/qtivoverlay, then tees the overlaid stream to Wayland display, MP4 recording, and qtirtspbin at /live. Uses one queued passthrough link into qtimetamux, runtime-expanded HOME model/labels paths, and 4/4 encoder IO after the transformed USB stream.", "path": "cpp-app/qimsdk_test_usb_camera_multi_output", "task": "snapshot-multi-output"}
{"prompt": "Synthetic test video source with string-form StreamFilter caps and geometric transform, C++ app to display. Generates videotestsrc ball-pattern frames, applies qtivtransform rotate=180, uses raw caps-string qti::StreamFilter objects (video/x-raw,format=NV12,width=1920,height=1080,framerate=30/1 then a resize caps string) instead of the VideoFilter builder, renders on waylandsink fullscreen. Use when raw GStreamer caps strings are needed instead of the typed VideoFilter wrapper.", "path": "cpp-app/qimsdk_test_videotestsrc_streamfilter_display", "task": "generic-camera-demo"}
{"prompt": "Runs a QIM SDK C++ pipeline entirely from an external YAML configuration file passed as a command-line argument, with no hand-coded topology in main.cc. Reads the YAML file into a string, constructs a qti::Pipeline directly from the YAML config text via the two-argument Pipeline constructor, and executes it. Use for the YAML-config-driven C++ app pattern where the pipeline topology and properties live entirely in an external YAML file rather than being expressed as Element/Pipeline calls.", "path": "cpp-app/qimsdk_test_yml", "task": "wayland-composition"}
{"path": "cpp-app/qimsdk_test_batched_multistream_yolov8_grid", "task": "multistream-grid", "prompt": "12-stream batched YOLOv8 object-detection AI wall C++ app from one MP4 file to a Wayland display. 12 independent decode chains are grouped 4-at-a-time into 3 batch groups; each group shares ONE qtimltflite HTP/NPU inference instance via qtibatch (mux 4 streams) / qtimldemux (split back to 4 per-stream results), avoiding 12 independent HTP graphs (error 6020). Each stream keeps its own qtimlpostprocess(module=yolov8, confidence 51.0) rendering an RGBA detection mask, plus its own raw passthrough branch off the per-stream tee; both feed interleaved qtivcomposer sink pads (2*i passthrough, 2*i+1 mask, same position/dimensions) tiled 4x3. Round-robins htp_device_id across HTP cores, raises RLIMIT_NOFILE, waylandsink sync=false. Two device-verified construction rules: the batch-group demux is named mldemux_<g> (never demux_<g>) to avoid a name collision with the per-stream qtdemux elements named demux_0..demux_11, and BOTH tee legs (split->qpass and split->qai) are linked with separate explicit pipeline.link() calls. Use for shared/batched inference across many streams; use the non-batched yolox grid instead for one independent inference instance per stream."}
{"path": "cpp-app/qimsdk_test_batched_singlegroup_yolov8", "task": "multistream-grid", "prompt": "Single-batch-group batched YOLOv8 object-detection C++ app from one MP4 file to a Wayland display: 4 decode streams share ONE qtimltflite HTP/NPU inference instance via qtibatch (mux 4 streams) / qtimldemux (split back to 4 per-stream results). Each stream keeps its own qtimlpostprocess(module=yolov8, confidence 51.0) RGBA detection mask plus a raw passthrough branch off its tee; both feed interleaved qtivcomposer sink pads (2*i passthrough, 2*i+1 mask) tiled 4x1. This is the minimal one-group shape of the batched multi-stream pattern (stack-local qti::Element wrappers, pipeline.execute()). Two device-verified construction rules: the batch demux is named mldemux_0 (never demux_0) to avoid colliding with per-stream qtdemux demux_0..demux_3, and BOTH tee legs (split->qpass and split->qai) are linked with separate explicit pipeline.link() calls. Use for a small batched-inference wall or as the reference for the batch pattern before scaling to multiple groups."}
